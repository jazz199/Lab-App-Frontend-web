1. instala reac native expo con:
    npm install --global expo-cli 
    //o
    npm install -g expo-cli
    // Verifica la instalacion 
    expo --version
2. clona el repositorio:
    git clone https://github.com/jazielSlayer/App-USB-Frontend.git
3. ejecuta el siguiente comando dentro del proyecto para instalar las dependencias
    npm install
4. para ejecutar el proyecto ejecuta el siguiente comando en la carpeta raiz del proyecto
    npm start
para jenerar la apk
expo build:android
# o
expo build:ios
ojo esto funciona solo con el bakent para instalarlo tedejo a quidebajo una guia

//////////////////////////////
Guia de instalcion de bakent si no lo a instaldo
1. instalar node js desde https://nodejs.org/es
puedes ver si esta instalado con: 
    node -v
    npm -v
2. descarga mysql o xampp
3. clona el repositorio con:
    git clone https://github.com/jazielSlayer/App-USB-bakent.git
4. instala las dependencias:
    npm install
5. crea un archivo llamado .env en la raiz del proyecto que contenga lo siguiente:
    DB_HOST=localhost
    DB_USER=tu_usuario
    DB_PASSWORD=tu_contraseña
    DB_DATABASE=LaboratorioDB
6. crea la base de datos ejecutando el script db
7. compila el proyecto con:
      npm run build //esto con pila el archivo src a dist
8. ejecuta el proyecto con:
    npm run dev //reinicia el servidor al hacer cambios
    npm start //ejecuta los archivos compilados en dist

  
